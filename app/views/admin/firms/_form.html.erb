<% form_for([:admin, @firm]) do |f| %>
	
  <p>
  	<%= render 'error_msg', :target => @firm %>  
  </p>
  <p>
    <%= f.label :Nazwa %><br />
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :Opis %><br />
    <%= f.text_area :description %>
  </p>
  
  <p>
  	<%= f.submit 'Zapisz' %>
  </p>
<% end %>